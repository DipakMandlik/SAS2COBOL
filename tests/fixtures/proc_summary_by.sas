/* Synthetic PROC SUMMARY test fixture */
PROC SORT DATA=LIB.SALES OUT=WORK.SALES_SORTED;
    BY REGION DIVISION;
RUN;

PROC SUMMARY DATA=WORK.SALES_SORTED NWAY;
    CLASS REGION DIVISION;
    VAR REVENUE;
    OUTPUT OUT=WORK.REG_SUM (DROP=_TYPE_ _FREQ_) SUM=TOTAL_REV;
RUN;
